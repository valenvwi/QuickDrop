<div>
<h2>We found a driver for your delivery</h2>
<h3>approximate drop-off time</h3>
<h2><%= @order.dropoff_at.strftime("%Y-%m-%d %H:%M") %> </h2>
</div>
<%# Status: <% if @order.status.nil? %>
<%# Confirmed %>
<%# <% else %>
<%# <%= @order.status %>
<%# <% end %>
<div class="card-body">
  <%= @order.driver %>
</div>
<button type="button" class="btn btn-secondary"  data-controller="popover" data-bs-container="body" data-bs-toggle="popover" data-bs-placement="right" data-bs-content = "0982309622" >
  Contact Driver
</button>
<div class="card" style="width: 18rem;">
  <div class="card-header">
    Drop-off Location
  </div>
  <ul class="list-group list-group-flush">
    <li class="list-group-item"><%= @order.dropoff_address %></li>
  </ul>
  <div class="card-header">
    Price
  </div>
  <ul class="list-group list-group-flush">
    <li class="list-group-item"><%= @order.price %></li>
  </ul>
</div>
<div class=“d-grid”>
  <%= button_to "Cancel Delivery", root_path , class:"btn btn-sticky btn-blue", method: :get %>
  <%= button_to "All orders", orders_path(@orders), class:"btn btn-sticky btn-blue", method: :get %>
</div>
